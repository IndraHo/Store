@model Store.Web.Models.ProductListViewModel

@{
    ViewBag.Title = "产品";
}

@foreach (var p in Model.Products)
{
    @Html.Partial("ProductSummary",p)
}
<div class="btn-group pull-right">@Html.PageLinks(Model.PaginationInfo,x=>Url.Action("List",new { page=x}))</div>

